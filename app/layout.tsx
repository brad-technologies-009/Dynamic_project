import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "FitCoach - Transform Your Body, Transform Your Life",
  description:
    "Join thousands who have achieved their fitness goals with our expert coaching and personalized programs. Professional fitness training, nutrition guidance, and lifestyle coaching.",
  keywords:
    "fitness, personal training, weight loss, strength training, HIIT, yoga, nutrition, wellness, health coaching",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
